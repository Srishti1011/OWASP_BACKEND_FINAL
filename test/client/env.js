// Production Environment Configuration
// This file sets the backend URL for your Railway deployment

window.ENV = {
    // Replace this with your actual Railway backend URL

    // BACKEND_URL: 'your_url'
};
